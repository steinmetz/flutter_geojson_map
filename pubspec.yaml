name: flutter_geojson_map
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  equatable: ^2.0.0
  flutter_bloc: ^9.1.0
  flutter_map: ^8.1.1
  font_awesome_flutter: ^10.8.0
  geojson_vi: ^2.2.5
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:  
  uses-material-design: true

dependency_overrides: